apiVersion: cert-manager.io/v1
kind: Certificate
metadata:
  name: tlscert
  namespace: plarail2023
spec:
  secretName: cloudflare-cert
  dnsNames:
    - "*.plarail.k1h.dev"
    - "plarail.k1h.dev"
  issuerRef:
    name: letsencrypt-prod
    kind: Issuer